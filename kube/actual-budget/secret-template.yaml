apiVersion: v1
kind: Secret
metadata:
  name: actual-budget-nathan
  namespace: actual-budget
type: Opaque
stringData:
  # Cloudflare Access OpenID credentials for Nathan's budget instance.
  # Values are in /tank/appdata/actual-budget/docker-compose.yml
  ACTUAL_OPENID_DISCOVERY_URL: "REPLACE"
  ACTUAL_OPENID_CLIENT_ID: "REPLACE"
  ACTUAL_OPENID_CLIENT_SECRET: "REPLACE"
---
apiVersion: v1
kind: Secret
metadata:
  name: actual-budget-cherise
  namespace: actual-budget
type: Opaque
stringData:
  # Cloudflare Access OpenID credentials for Cherise's budget instance.
  # Values are in /tank/appdata/actual-budget-cherise/docker-compose.yml
  ACTUAL_OPENID_DISCOVERY_URL: "REPLACE"
  ACTUAL_OPENID_CLIENT_ID: "REPLACE"
  ACTUAL_OPENID_CLIENT_SECRET: "REPLACE"
