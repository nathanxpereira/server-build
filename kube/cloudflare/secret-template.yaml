apiVersion: v1
kind: Secret
metadata:
  name: cloudflare-tunnel
  namespace: cloudflare
type: Opaque
stringData:
  # Paste your tunnel token from the Cloudflare dashboard.
  # Get it from: Zero Trust → Networks → Tunnels → your tunnel → Configure → Install connector
  token: "REPLACE_WITH_CLOUDFLARE_TUNNEL_TOKEN"
